Given a multimodal misinformation sample with multiple related images and one article text.

Article text is:
[News caption]

Provided external evidence candidates:
[Provided Evidence]

According to the full image set, determine whether the images as a group faithfully support reality or introduce misleading visual evidence. Please follow the instructions below:
Thought 1: Describe the important visual content across the full image set in no more than 8 short sentences, including any suspicious edits, synthetic content, misleading crops, mismatched scenes, or other cues that matter for factual judgment.
Observation: [Fact-conflicting Description]
Action 1: Based on the Observation, output exactly one final veracity label in the form:
- Finish[IMAGE_STRONG_SUPPORT]. if the image set is visually reliable and materially consistent with reality.
- Finish[IMAGE_WEAK_SUPPORT]. if the image set is mostly reliable, with only minor visual issues that do not change the overall impression.
- Finish[IMAGE_MIXED]. ONLY if the image set contains both important reliable AND important misleading visual elements that carry roughly equal weight. Do NOT use this label as a default or when uncertain; instead, lean toward SUPPORT or REFUTE based on which direction dominates.
- Finish[IMAGE_WEAK_REFUTE]. if the image set gives a mainly wrong impression but still contains some real visual elements.
- Finish[IMAGE_STRONG_REFUTE]. if the image set is largely fabricated, heavily edited, or visually misleading in a way that breaks the core visual claim.

In addition, identify any specific VISUAL DISTORTION categories that are present in the image set (you may select zero, one, or multiple). These categories are independent of the overall direction above, and apply only to the images:
- V1 Synthetic Visual Content: at least one image (or a clearly important part of one image) is generated by AI or otherwise fully synthetic (e.g. diffusion-generated photo, fake screenshot, AI-rendered scene, deepfake face).
- V2 Visual Editing: at least one image is locally edited/manipulated on top of a real photo (e.g. added text/labels, swapped object, retouched element, overlaid warning banner, color/lighting manipulation pushing a specific narrative).

Action 2: Output exactly one line listing the visual distortion labels you identified, in the form 'Distortions: V1, V2' (use the comma-separated codes V1, V2 in any combination). If none of these visual distortion categories applies, output 'Distortions: NONE'.

IMPORTANT:
1. Choose the veracity label that best matches the dominant direction. IMAGE_MIXED should only be used when both supporting and refuting visual evidence are genuinely important and roughly balanced. When in doubt between MIXED and another label, prefer the non-MIXED label that matches the dominant impression.
2. The Distortions line is independent: a sample can be labeled IMAGE_WEAK_SUPPORT and still legitimately carry a 'V2' tag if there is a real but minor edit. Only include a distortion code when you have a concrete visual basis for it.
3. The two output lines 'Finish[...]' and 'Distortions: ...' must both appear, on separate lines, after your description.
