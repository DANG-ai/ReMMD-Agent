# =============================================================================
# Runtime dependencies for the unified MMD-Agent
# (5cls verdict + 8-label distortion taxonomy on ReMMDBench).
#
# Tested with Python 3.10 in the `mmd` conda env on the evaluation server.
# Install with:
#   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
#
# - httpx / tqdm  : core IO + progress bar.
# - numpy         : pure-Python metric helpers + matrix manipulation.
# - matplotlib    : confusion-matrix heatmap and per-label bar plot.
# - seaborn       : publication-grade colour palettes / styling.
# - pandas        : tabular summary tables for the eval_summary report.
# Torch / scikit-learn are intentionally NOT required: every metric is
# implemented in pure Python so the env stays lightweight on the server.
# =============================================================================
httpx>=0.27,<1.0
tqdm>=4.65
numpy>=1.24,<2.0
matplotlib>=3.7
seaborn>=0.13
pandas>=2.0
